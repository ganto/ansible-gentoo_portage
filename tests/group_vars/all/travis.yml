---

# Clean Travis environment variables
gentoo_portage__makeconf_vars:
  ENV_UNSET: >-
    ELIXIR_VERSION GEM_* GOPATH GOROOT JAVA_HOME JRUBY_OPTS MERB_ENV NVM_*
    PERLBREW_* PIP_DISABLE_PIP_VERSION_CHECK PYENV_* PYTHON_CFLAGS
    PYTHON_CONFIGURE_OPTS RACK_ENV RAILS_ENV RBENV_SHELL RUBY_VERSION rvm_bin_path
    rvm_version VIRTUAL_ENV
